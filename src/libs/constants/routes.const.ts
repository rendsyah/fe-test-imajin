export class Routes {
  // AUTH
  static readonly AUTH = '/auth';
  static readonly AUTH_LOGIN = Routes.AUTH + '/login';
  static readonly AUTH_REGISTER = Routes.AUTH + '/register';

  // CARTS
  static readonly CARTS = '/carts';

  // PRODUCTS
  static readonly PRODUCTS = '/products';
  static readonly PRODUCTS_DETAIL = Routes.PRODUCTS + '/detail';

  // TRANSACTIONS
  static readonly TRANSACTION = '/transaction';
}
